# Copy to .env and set to 1 to use the mocked in-proxy (no real Psiphon
# resources). Lets you test the UI on iOS/Android without ios_embedded_server_entries
# or psiphon config. For development only.
# EXPO_PUBLIC_USE_MOCK_INPROXY=1
